#!/bin/bash
EMOJI=`sed '/^#/d' ~/.local/share/emoji | dmenu -l 15 | awk '{print $1}'`
echo $EMOJI | xclip -i -selection clipboard
[ -n $EMOJI ] && notify-send "$EMOJI has been copied to your clipboard"
